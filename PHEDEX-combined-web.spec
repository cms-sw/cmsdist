### RPM cms PHEDEX-combined-web 3

# This is a fake spec whose only job is to build PHEDEX-web and
# PHEDEX-datasvc on a combined platform of dependencies

Requires: PHEDEX-web PHEDEX-webapp PHEDEX-datasvc

%prep
%build
%install
