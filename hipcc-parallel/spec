### RPM external hipcc-parallel 260921.1
## NOCOMPILER
## NO_VERSION_SUFFIX
%define tag 090e11dfbe6bc7e018feed43e4f33aacd26ff5fe
Source: git+https://github.com/cms-patatrack/%{n}.git?obj=master/%{tag}&export=%{n}-%{realversion}&output=/%{n}-%{realversion}-%{tag}.tgz

%prep
%setup -n %{n}-%{realversion}

%build

%install
mkdir %{i}/bin
install hipcc %{i}/bin/hipcc-parallel
chmod a+x %{i}/bin/hipcc-parallel
