#!/bin/bash
for a in "$@"; do
  if [[ "$a" == -j* ]]; then
    exec ninja.exe "$@"
  fi
done
args=()
if [[ -n "${NINJA_NUM_JOBS}" ]]; then
  args=(-j "${NINJA_NUM_JOBS}")
fi
exec ninja.exe "${args[@]}" "$@"
