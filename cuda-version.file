%define cuda_version 12.2.1
## INITENV +PATH LD_LIBRARY_PATH %i/lib64
%define cuda_install_dir /cvmfs/projects.cern.ch/cms-restricted/%{_arch}/rhel%{rhel}/external/cuda/%{cuda_version}

%prep
if [ ! -d %{cuda_install_dir} ] ; then
  echo "ERROR: Missing cuda installation at %{cuda_install_dir}"
  exit 1
fi

%build
