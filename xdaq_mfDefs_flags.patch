--- orig/xdaq/VR15544-cms/xdaq/config/mfDefs.linux	2009-12-05 01:14:47.000000000 +0100
+++ ptch/xdaq/VR15544-cms/xdaq/config/mfDefs.linux	2009-12-07 15:53:28.000000000 +0100
@@ -40,22 +40,13 @@
 #
 # Compilation flags for 64 bit Linux
 #
-ifneq (,$(findstring x86_64,$(XDAQ_PLATFORM)))
 CFlags			= $(DebugFlags) -fPIC
 CCFlags			= -g -Wall -fPIC
-else
-CFlags			= $(DebugFlags) 
-CCFlags			= -g -Wall
-endif
 
 #
 # Linking flags for 64 bit Linux
 #
-ifneq (,$(findstring x86_64,$(XDAQ_PLATFORM)))
 DynamicLinkFlags	= -shared -fPIC
-else
-DynamicLinkFlags	= -shared
-endif
 
 ExecutableLinkFlags	= 
 StaticLinkFlags		=
