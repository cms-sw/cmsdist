#!/bin/sh -e

if [ "X$1" = X ]; then
  echo "Please specify a check out area to migrate to the new CMS CVSROOT"
fi

REPO=$1

find "$REPO" -path "*/CVS/Root" -exec perl -p -i -e 's|:/cvs/CMSSW|:/local/reps/CMSSW|;s|:/cvs_server/repositories/CMSSW|:/local/reps/CMSSW|;' {} \;
